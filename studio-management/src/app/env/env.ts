export const environment = {
  // Local-only. Switch to the LAN IP (e.g. 'http://192.168.1.73:3000/') to test
  // from a phone / other device on the same WiFi.
  apiUrl: 'http://localhost:3000/',
};
